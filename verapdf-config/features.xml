<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<featuresConfig>
  <enabledFeatures>
    <!-- Esto habilita la extracción de metadatos XMP/Dublin Core -->
    <feature>METADATA</feature>
    <!-- Esto habilita la extracción de imágenes -->
    <feature>IMAGES</feature>
    <!-- Esto habilita la extracción de estructura interna (headings, figuras, etc.) -->
    <feature>STRUCTURE</feature>
  </enabledFeatures>
</featuresConfig>
